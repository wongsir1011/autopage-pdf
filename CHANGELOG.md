# 更新紀錄

## 1.3.0

### 新增

- 使用本機 Tesseract OCR 建立保留原圖的可搜尋 PDF。
- 可選擇繁體中文、簡體中文、英文及中英混合辨識。
- 可同步輸出 UTF-8 純文字 TXT。
- OCR 專用自動對比、灰階、黑白二值化及不處理模式。
- GUI 內建 Tesseract 執行檔、版本及語言包檢查。
- OCR 逐頁進度、最大輸出頁數估算及完成提示音。
- 自動記住輸出、翻頁、裁切、分頁及 OCR 設定。
- OCR 搜尋文字層及失敗容錯的自動測試。

### 改善

- 單頁 OCR 失敗不會中止整份文件；該頁仍以原始圖片輸出，完成後列出失敗頁碼。
- OCR 影像增強只套用於辨識副本，不改變 PDF 可見頁面的畫質。
- 一般圖片 PDF 保持為預設，未安裝 Tesseract 仍可使用 v1.2.0 的全部功能。
- PDF 先寫入同一輸出資料夾的暫存檔再原子取代，減少中途失敗留下不完整檔案。

## 1.2.0

### 新增

- 自動偵測閱讀頁面邊界並裁走外圍背景。
- 手動設定左、上、右、下裁切像素。
- 雙頁畫面自動偵測中央書脊／留白並分割。
- 固定正中央雙頁分割後備模式。
- 由左至右及由右至左的 PDF 頁面順序。
- 正式擷取前的原圖、裁切及分頁預覽。
- 「停止並輸出」按鈕。
- 影像處理自動測試。

### 改善

- 保留 v1.1.0 的單頁、不裁切輸出為預設，避免升級後意外改變結果。
- GUI 更新改由 Tkinter 主執行緒處理，減少長時間擷取時的介面不穩定。
- 暫存目錄改用每次工作獨立的系統暫存資料夾，避免舊截圖混入新 PDF。
- 末頁重複確認不會把重複頁加入 PDF。
- 錯誤時保留暫存圖片，方便復原及診斷。
- Windows BAT 改用純 ASCII 指令及 CRLF 換行，避免繁體 Windows 將 UTF-8 內容誤當成指令。

## 1.1.0

- 圖形介面及互動校準。
- 0.5–2.0 秒翻頁間隔。
- 鍵盤、Page Down 及滑鼠翻頁。
- macOS AppleScript 翻頁。
- 連續畫面重複的智慧末頁停止。
- Windows／macOS 一鍵啟動及 Windows EXE 打包腳本。
